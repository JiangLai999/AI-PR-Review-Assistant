window.__WEBSITE_DOCS__ = {"tabs":[{"id":"quickstart","label":"快速开始","title":"README · 快速开始","source":"README.md","html":"<h4>推荐安装：PyPI</h4>\n<p>CLI 工具最推荐使用 <code>pipx</code> 安装。它会自动创建隔离环境，并把 <code>pr-review</code> 命令暴露到 PATH。</p>\n<div class=\"code-block\"><pre><code>pipx install ai-pr-review</code></pre></div>\n<p>如果你已经在自己的虚拟环境里，也可以使用：</p>\n<div class=\"code-block\"><pre><code>pip install ai-pr-review</code></pre></div>\n<h4>GitHub 源安装</h4>\n<p>适合需要最新主分支代码、还没发布到 PyPI 的修复版本：</p>\n<div class=\"code-block\"><pre><code> pipx install &quot;git+https://github.com/JiangLai999/AI-PR-Review-Assistant.git&quot;</code></pre></div>\n<h4>一行命令安装</h4>\n<p>Linux / macOS，默认从 PyPI 安装：</p>\n<div class=\"code-block\"><pre><code> curl -fsSL https://raw.githubusercontent.com/JiangLai999/AI-PR-Review-Assistant/main/install.sh | sh</code></pre></div>\n<p>Linux / macOS，强制从 GitHub 安装：</p>\n<div class=\"code-block\"><pre><code> curl -fsSL https://raw.githubusercontent.com/JiangLai999/AI-PR-Review-Assistant/main/install.sh | INSTALL_SOURCE=github GITHUB_REPOSITORY=JiangLai999/AI-PR-Review-Assistant sh</code></pre></div>\n<p>Windows PowerShell，默认从 PyPI 安装：</p>\n<div class=\"code-block\"><pre><code> irm https://raw.githubusercontent.com/JiangLai999/AI-PR-Review-Assistant/main/install.ps1 | iex</code></pre></div>\n<p>Windows PowerShell，强制从 GitHub 安装：</p>\n<div class=\"code-block\"><pre><code> $env:INSTALL_SOURCE=&#x27;github&#x27;; $env:GITHUB_REPOSITORY=&#x27;JiangLai999/AI-PR-Review-Assistant&#x27;; irm https://raw.githubusercontent.com/JiangLai999/AI-PR-Review-Assistant/main/install.ps1 | iex</code></pre></div>\n<p>安装完成后：</p>\n<div class=\"code-block\"><pre><code>pr-review --help\npr-review config</code></pre></div>\n<h4>从源码安装</h4>\n<div class=\"code-block\"><pre><code> git clone https://github.com/JiangLai999/AI-PR-Review-Assistant.git\n cd AI-PR-Review-Assistant\npip install -e .</code></pre></div>\n<p>开发环境：</p>\n<div class=\"code-block\"><pre><code>pip install -e .[dev]</code></pre></div>\n<h4>配置优先级</h4>\n<p><code>pr-review</code> 现在支持分层配置，推荐优先使用“项目配置 + 本地私有覆盖 + 环境变量”的组合，而不是把所有敏感信息都写进全局配置文件。</p>\n<p>默认加载顺序如下，后者覆盖前者：</p>\n<ol><li>用户级配置文件</li><li>项目级 <code>.ai_pr_review/config.json</code></li><li>项目本地私有配置 <code>.ai_pr_review/config.local.json</code></li><li>环境变量</li><li>命令行 <code>--config &lt;path&gt;</code> 指定的显式配置文件</li></ol>\n<p>可以通过 <code>pr-review config show</code> 查看当前解析后的配置以及实际使用的配置来源。</p>\n<h4>必需环境变量</h4>\n<p>至少需要：</p>\n<ul><li><code>GITHUB_TOKEN</code>：用于访问 GitHub PR 数据。</li><li>一个模型供应商 API Key：默认 provider 是 <code>anthropic</code>，如果切换 provider，则使用对应环境变量。</li></ul>\n<p>常见 provider 对应的环境变量：</p>\n<ul><li><code>ANTHROPIC_API_KEY</code></li><li><code>OPENAI_API_KEY</code></li><li><code>DEEPSEEK_API_KEY</code></li><li><code>OPENROUTER_API_KEY</code></li><li><code>DASHSCOPE_API_KEY</code></li><li><code>SILICONFLOW_API_KEY</code></li><li><code>MOONSHOT_API_KEY</code></li><li><code>ZHIPUAI_API_KEY</code></li><li><code>BAICHUAN_API_KEY</code></li><li><code>MINIMAX_API_KEY</code></li><li><code>STEPFUN_API_KEY</code></li><li><code>ARK_API_KEY</code></li><li><code>HUNYUAN_API_KEY</code></li><li><code>YI_API_KEY</code></li><li><code>MODEL_PROVIDER_API_KEY</code>：<code>custom</code> provider 使用</li></ul>\n<p>也支持这些通用覆盖变量：</p>\n<ul><li><code>AI_PR_REVIEW_CONFIG</code></li><li><code>AI_PR_REVIEW_PROVIDER</code></li><li><code>AI_PR_REVIEW_MODEL</code></li><li><code>AI_PR_REVIEW_API_KEY</code></li><li><code>AI_PR_REVIEW_BASE_URL</code></li><li><code>AI_PR_REVIEW_API_FORMAT</code></li></ul>\n<p>示例：</p>\n<div class=\"code-block\"><pre><code>export GITHUB_TOKEN=&quot;your_github_token&quot;\nexport ANTHROPIC_API_KEY=&quot;your_anthropic_api_key&quot;</code></pre></div>\n<p><code>custom</code> provider 示例：</p>\n<div class=\"code-block\"><pre><code>export GITHUB_TOKEN=&quot;your_github_token&quot;\nexport MODEL_PROVIDER_API_KEY=&quot;your_custom_api_key&quot;\nexport AI_PR_REVIEW_PROVIDER=&quot;custom&quot;\nexport AI_PR_REVIEW_MODEL=&quot;mimo v2.5pro&quot;\nexport AI_PR_REVIEW_BASE_URL=&quot;https://token-plan-cn.xiaomimimo.com/v1&quot;</code></pre></div>\n<h4>配置模型供应商</h4>\n<p>项目默认 provider 是 <code>anthropic</code>，也可以通过交互式命令写入配置：</p>\n<div class=\"code-block\"><pre><code># 启动图形化配置向导（推荐）\npr-review config\n\n# 快速配置\npr-review config --quick\n\n# 查看配置\npr-review config show\n\n# 测试配置\npr-review config test</code></pre></div>\n<p>**配置向导特点**：</p>\n<ul><li>🎨 美观的 Rich 面板界面</li><li>📊 供应商选择表格（显示类型、推荐度）</li><li>🔑 API Key 输入支持粘贴</li><li>✅ 配置验证进度条</li><li>📋 配置摘要展示</li></ul>\n<p>如果是在仓库内初始化团队共享配置，推荐使用：</p>\n<div class=\"code-block\"><pre><code># 生成 .ai_pr_review/config.json、config.local.json.example，并更新 .gitignore\npr-review config init --provider deepseek\n\n# 国内聚合模型示例\npr-review config init --provider siliconflow\n\n# 自定义 OpenAI 兼容服务示例\npr-review config init \\\n  --provider custom \\\n  --model &quot;mimo v2.5pro&quot; \\\n  --base-url &quot;https://token-plan-cn.xiaomimimo.com/v1&quot; \\\n  --api-format openai</code></pre></div>\n<p><code>config init</code> 默认不会把 API Key 写入项目配置文件。它会生成 <code>.ai_pr_review/config.local.json.example</code>，并把 <code>.ai_pr_review/config.local.json</code> 加入 <code>.gitignore</code>。</p>\n<p>交互式向导第一步会询问界面语言、聊天布局和模型回复语言。当前支持中文和英文，后续也可以通过 <code>pr-review preferences</code> 调整。</p>\n<p>交互式向导会在最后明确询问是否保存 API Key。默认选择“是”，这样完成配置后可以直接运行审查命令。</p>\n<p>如果你不希望 API Key 写入配置文件，可以显式使用：</p>\n<div class=\"code-block\"><pre><code>pr-review config --quick --no-save-key</code></pre></div>\n<p>选择不保存时，后续运行必须通过环境变量提供 API Key，例如 <code>DEEPSEEK_API_KEY</code>、<code>MODEL_PROVIDER_API_KEY</code> 或 <code>AI_PR_REVIEW_API_KEY</code>。</p>\n<p>更推荐的实践是：</p>\n<ol><li>把团队共享配置放进项目根目录下的 <code>.ai_pr_review/config.json</code></li><li>把个人私有覆盖放进 <code>.ai_pr_review/config.local.json</code></li><li>把 API Key 放进环境变量</li></ol>\n<p>示例项目配置：</p>\n<div class=\"code-block\"><pre><code>{\n  &quot;provider&quot;: {\n    &quot;name&quot;: &quot;custom&quot;,\n    &quot;display_name&quot;: &quot;Custom Endpoint&quot;,\n    &quot;base_url&quot;: &quot;https://token-plan-cn.xiaomimimo.com/v1&quot;,\n    &quot;api_format&quot;: &quot;openai&quot;,\n    &quot;models&quot;: {\n      &quot;mimo v2.5pro&quot;: {\n        &quot;name&quot;: &quot;mimo v2.5pro&quot;,\n        &quot;context_window&quot;: 32768,\n        &quot;max_output&quot;: 4096\n      }\n    },\n    &quot;default_model&quot;: &quot;mimo v2.5pro&quot;\n  },\n  &quot;preferences&quot;: {\n    &quot;output_format&quot;: &quot;terminal&quot;,\n    &quot;language&quot;: &quot;zh-CN&quot;,\n    &quot;auto_publish_comment&quot;: false\n  }\n}</code></pre></div>\n<p>如果要针对单次命令使用自定义配置文件，也可以：</p>\n<div class=\"code-block\"><pre><code>pr-review --config ./.ai_pr_review/config.local.json config show\npr-review --config ./custom-config.json https://github.com/owner/repo/pull/123</code></pre></div>\n<p>当前内置预设包括：<code>anthropic</code>、<code>openai</code>、<code>deepseek</code>、<code>qwen</code>、<code>siliconflow</code>、<code>moonshot</code>、<code>zhipu</code>、<code>baichuan</code>、<code>minimax</code>、<code>stepfun</code>、<code>doubao</code>、<code>hunyuan</code>、<code>yi</code>、<code>openrouter</code>、<code>api2d</code>、<code>closeai</code>、<code>ohmygpt</code>、<code>custom</code>。</p>\n<h4>使用</h4>\n<div class=\"code-block\"><pre><code># 基本用法\npr-review https://github.com/owner/repo/pull/123\n\n# 指定模型\npr-review https://github.com/owner/repo/pull/123 --model claude-sonnet-4-20250514\n\n# 输出 Markdown 报告\npr-review https://github.com/owner/repo/pull/123 --format markdown --output report.md\n\n# 输出 JSON 报告\npr-review https://github.com/owner/repo/pull/123 --format json --output report.json\n\n# 仅抓取 PR 元数据\npr-review https://github.com/owner/repo/pull/123 --only-fetch\n\n# 仅执行过滤并查看原因\npr-review https://github.com/owner/repo/pull/123 --only-filter --show-filter-reasons\n\n# 发布评论到 GitHub PR\npr-review https://github.com/owner/repo/pull/123 --publish-comment --format markdown\n\n# 查看历史记录与统计\npr-review history --limit 20\npr-review stats\n\n# 使用自定义配置路径\npr-review --config ./.ai_pr_review/config.local.json config show\n\n# 初始化项目级模型配置\npr-review config init --provider siliconflow\n\n# 调整界面语言、模型回复语言和聊天布局\npr-review preferences --ui-language zh-CN --response-language zh-CN --chat-layout compact\n\n# 进入轻量终端聊天框\npr-review chat\n\n# 单次发送一条聊天消息\npr-review chat --message &quot;帮我总结这个项目的审查重点&quot;\n\n# 如果服务商提示模型不支持，持久切换模型 ID\npr-review config model --name &quot;服务商实际支持的模型ID&quot;\n\n# 尝试从当前 base_url + api_key 自动发现模型列表\npr-review config models\n\n# 自动把远端返回的第一个模型设置为默认模型\npr-review config models --set-first\n\n# 仅本次聊天临时切换模型 ID\npr-review chat --model &quot;服务商实际支持的模型ID&quot; --message &quot;你好&quot;\n\n# custom provider 通过环境变量运行\nMODEL_PROVIDER_API_KEY=&quot;your_custom_api_key&quot; \\\nAI_PR_REVIEW_PROVIDER=&quot;custom&quot; \\\nAI_PR_REVIEW_MODEL=&quot;mimo v2.5pro&quot; \\\nAI_PR_REVIEW_BASE_URL=&quot;https://token-plan-cn.xiaomimimo.com/v1&quot; \\\npr-review https://github.com/owner/repo/pull/123</code></pre></div>"},{"id":"config-priority","label":"配置优先级","title":"README · 配置优先级","source":"README.md","html":"<p><code>pr-review</code> 现在支持分层配置，推荐优先使用“项目配置 + 本地私有覆盖 + 环境变量”的组合，而不是把所有敏感信息都写进全局配置文件。</p>\n<p>默认加载顺序如下，后者覆盖前者：</p>\n<ol><li>用户级配置文件</li><li>项目级 <code>.ai_pr_review/config.json</code></li><li>项目本地私有配置 <code>.ai_pr_review/config.local.json</code></li><li>环境变量</li><li>命令行 <code>--config &lt;path&gt;</code> 指定的显式配置文件</li></ol>\n<p>可以通过 <code>pr-review config show</code> 查看当前解析后的配置以及实际使用的配置来源。</p>\n<p>#</p>"},{"id":"env","label":"环境变量","title":"README · 必需环境变量","source":"README.md","html":"<p>至少需要：</p>\n<ul><li><code>GITHUB_TOKEN</code>：用于访问 GitHub PR 数据。</li><li>一个模型供应商 API Key：默认 provider 是 <code>anthropic</code>，如果切换 provider，则使用对应环境变量。</li></ul>\n<p>常见 provider 对应的环境变量：</p>\n<ul><li><code>ANTHROPIC_API_KEY</code></li><li><code>OPENAI_API_KEY</code></li><li><code>DEEPSEEK_API_KEY</code></li><li><code>OPENROUTER_API_KEY</code></li><li><code>DASHSCOPE_API_KEY</code></li><li><code>SILICONFLOW_API_KEY</code></li><li><code>MOONSHOT_API_KEY</code></li><li><code>ZHIPUAI_API_KEY</code></li><li><code>BAICHUAN_API_KEY</code></li><li><code>MINIMAX_API_KEY</code></li><li><code>STEPFUN_API_KEY</code></li><li><code>ARK_API_KEY</code></li><li><code>HUNYUAN_API_KEY</code></li><li><code>YI_API_KEY</code></li><li><code>MODEL_PROVIDER_API_KEY</code>：<code>custom</code> provider 使用</li></ul>\n<p>也支持这些通用覆盖变量：</p>\n<ul><li><code>AI_PR_REVIEW_CONFIG</code></li><li><code>AI_PR_REVIEW_PROVIDER</code></li><li><code>AI_PR_REVIEW_MODEL</code></li><li><code>AI_PR_REVIEW_API_KEY</code></li><li><code>AI_PR_REVIEW_BASE_URL</code></li><li><code>AI_PR_REVIEW_API_FORMAT</code></li></ul>\n<p>示例：</p>\n<div class=\"code-block\"><pre><code>export GITHUB_TOKEN=&quot;your_github_token&quot;\nexport ANTHROPIC_API_KEY=&quot;your_anthropic_api_key&quot;</code></pre></div>\n<p><code>custom</code> provider 示例：</p>\n<div class=\"code-block\"><pre><code>export GITHUB_TOKEN=&quot;your_github_token&quot;\nexport MODEL_PROVIDER_API_KEY=&quot;your_custom_api_key&quot;\nexport AI_PR_REVIEW_PROVIDER=&quot;custom&quot;\nexport AI_PR_REVIEW_MODEL=&quot;mimo v2.5pro&quot;\nexport AI_PR_REVIEW_BASE_URL=&quot;https://token-plan-cn.xiaomimimo.com/v1&quot;</code></pre></div>\n<p>#</p>"},{"id":"provider-config","label":"Provider 配置","title":"README · 配置模型供应商","source":"README.md","html":"<p>项目默认 provider 是 <code>anthropic</code>，也可以通过交互式命令写入配置：</p>\n<div class=\"code-block\"><pre><code># 启动图形化配置向导（推荐）\npr-review config\n\n# 快速配置\npr-review config --quick\n\n# 查看配置\npr-review config show\n\n# 测试配置\npr-review config test</code></pre></div>\n<p>**配置向导特点**：</p>\n<ul><li>🎨 美观的 Rich 面板界面</li><li>📊 供应商选择表格（显示类型、推荐度）</li><li>🔑 API Key 输入支持粘贴</li><li>✅ 配置验证进度条</li><li>📋 配置摘要展示</li></ul>\n<p>如果是在仓库内初始化团队共享配置，推荐使用：</p>\n<div class=\"code-block\"><pre><code># 生成 .ai_pr_review/config.json、config.local.json.example，并更新 .gitignore\npr-review config init --provider deepseek\n\n# 国内聚合模型示例\npr-review config init --provider siliconflow\n\n# 自定义 OpenAI 兼容服务示例\npr-review config init \\\n  --provider custom \\\n  --model &quot;mimo v2.5pro&quot; \\\n  --base-url &quot;https://token-plan-cn.xiaomimimo.com/v1&quot; \\\n  --api-format openai</code></pre></div>\n<p><code>config init</code> 默认不会把 API Key 写入项目配置文件。它会生成 <code>.ai_pr_review/config.local.json.example</code>，并把 <code>.ai_pr_review/config.local.json</code> 加入 <code>.gitignore</code>。</p>\n<p>交互式向导第一步会询问界面语言、聊天布局和模型回复语言。当前支持中文和英文，后续也可以通过 <code>pr-review preferences</code> 调整。</p>\n<p>交互式向导会在最后明确询问是否保存 API Key。默认选择“是”，这样完成配置后可以直接运行审查命令。</p>\n<p>如果你不希望 API Key 写入配置文件，可以显式使用：</p>\n<div class=\"code-block\"><pre><code>pr-review config --quick --no-save-key</code></pre></div>\n<p>选择不保存时，后续运行必须通过环境变量提供 API Key，例如 <code>DEEPSEEK_API_KEY</code>、<code>MODEL_PROVIDER_API_KEY</code> 或 <code>AI_PR_REVIEW_API_KEY</code>。</p>\n<p>更推荐的实践是：</p>\n<ol><li>把团队共享配置放进项目根目录下的 <code>.ai_pr_review/config.json</code></li><li>把个人私有覆盖放进 <code>.ai_pr_review/config.local.json</code></li><li>把 API Key 放进环境变量</li></ol>\n<p>示例项目配置：</p>\n<div class=\"code-block\"><pre><code>{\n  &quot;provider&quot;: {\n    &quot;name&quot;: &quot;custom&quot;,\n    &quot;display_name&quot;: &quot;Custom Endpoint&quot;,\n    &quot;base_url&quot;: &quot;https://token-plan-cn.xiaomimimo.com/v1&quot;,\n    &quot;api_format&quot;: &quot;openai&quot;,\n    &quot;models&quot;: {\n      &quot;mimo v2.5pro&quot;: {\n        &quot;name&quot;: &quot;mimo v2.5pro&quot;,\n        &quot;context_window&quot;: 32768,\n        &quot;max_output&quot;: 4096\n      }\n    },\n    &quot;default_model&quot;: &quot;mimo v2.5pro&quot;\n  },\n  &quot;preferences&quot;: {\n    &quot;output_format&quot;: &quot;terminal&quot;,\n    &quot;language&quot;: &quot;zh-CN&quot;,\n    &quot;auto_publish_comment&quot;: false\n  }\n}</code></pre></div>\n<p>如果要针对单次命令使用自定义配置文件，也可以：</p>\n<div class=\"code-block\"><pre><code>pr-review --config ./.ai_pr_review/config.local.json config show\npr-review --config ./custom-config.json https://github.com/owner/repo/pull/123</code></pre></div>\n<p>当前内置预设包括：<code>anthropic</code>、<code>openai</code>、<code>deepseek</code>、<code>qwen</code>、<code>siliconflow</code>、<code>moonshot</code>、<code>zhipu</code>、<code>baichuan</code>、<code>minimax</code>、<code>stepfun</code>、<code>doubao</code>、<code>hunyuan</code>、<code>yi</code>、<code>openrouter</code>、<code>api2d</code>、<code>closeai</code>、<code>ohmygpt</code>、<code>custom</code>。</p>\n<p>#</p>"},{"id":"usage","label":"使用示例","title":"README · 使用","source":"README.md","html":"<div class=\"code-block\"><pre><code># 基本用法\npr-review https://github.com/owner/repo/pull/123\n\n# 指定模型\npr-review https://github.com/owner/repo/pull/123 --model claude-sonnet-4-20250514\n\n# 输出 Markdown 报告\npr-review https://github.com/owner/repo/pull/123 --format markdown --output report.md\n\n# 输出 JSON 报告\npr-review https://github.com/owner/repo/pull/123 --format json --output report.json\n\n# 仅抓取 PR 元数据\npr-review https://github.com/owner/repo/pull/123 --only-fetch\n\n# 仅执行过滤并查看原因\npr-review https://github.com/owner/repo/pull/123 --only-filter --show-filter-reasons\n\n# 发布评论到 GitHub PR\npr-review https://github.com/owner/repo/pull/123 --publish-comment --format markdown\n\n# 查看历史记录与统计\npr-review history --limit 20\npr-review stats\n\n# 使用自定义配置路径\npr-review --config ./.ai_pr_review/config.local.json config show\n\n# 初始化项目级模型配置\npr-review config init --provider siliconflow\n\n# 调整界面语言、模型回复语言和聊天布局\npr-review preferences --ui-language zh-CN --response-language zh-CN --chat-layout compact\n\n# 进入轻量终端聊天框\npr-review chat\n\n# 单次发送一条聊天消息\npr-review chat --message &quot;帮我总结这个项目的审查重点&quot;\n\n# 如果服务商提示模型不支持，持久切换模型 ID\npr-review config model --name &quot;服务商实际支持的模型ID&quot;\n\n# 尝试从当前 base_url + api_key 自动发现模型列表\npr-review config models\n\n# 自动把远端返回的第一个模型设置为默认模型\npr-review config models --set-first\n\n# 仅本次聊天临时切换模型 ID\npr-review chat --model &quot;服务商实际支持的模型ID&quot; --message &quot;你好&quot;\n\n# custom provider 通过环境变量运行\nMODEL_PROVIDER_API_KEY=&quot;your_custom_api_key&quot; \\\nAI_PR_REVIEW_PROVIDER=&quot;custom&quot; \\\nAI_PR_REVIEW_MODEL=&quot;mimo v2.5pro&quot; \\\nAI_PR_REVIEW_BASE_URL=&quot;https://token-plan-cn.xiaomimimo.com/v1&quot; \\\npr-review https://github.com/owner/repo/pull/123</code></pre></div>"},{"id":"cli-api","label":"CLI API","title":"API 文档 · 主命令与配置命令","source":"docs/API.md","html":"<h4><code>pr-review &lt;PR_URL&gt;</code></h4>\n<p>对指定 GitHub Pull Request 执行完整审查。</p>\n<p>参数：</p>\n<ul><li><code>PR_URL</code>：GitHub Pull Request URL，例如 <code>https://github.com/owner/repo/pull/123</code></li></ul>\n<p>选项：</p>\n<ul><li><code>--model TEXT</code>：覆盖配置中的模型名。</li><li><code>--format [terminal|markdown|json]</code>：报告输出格式，默认 <code>terminal</code>。</li><li><code>--output FILE</code>：把报告写入目标文件。若文件后缀为 <code>.md</code> 或 <code>.json</code>，会自动推断输出格式。</li><li><code>--publish-comment</code>：将 GitHub 评论格式的报告发布到对应 PR。</li><li><code>--verbose</code>：显示逐文件处理进度。</li><li><code>--dry-run</code>：抓取与过滤 PR 文件，不调用模型。</li><li><code>--only-fetch</code>：只抓取 PR 元数据。</li><li><code>--only-filter</code>：抓取并过滤 PR 文件。</li><li><code>--show-filter-reasons</code>：在 <code>--dry-run</code> 或 <code>--only-filter</code> 下显示每个文件的过滤原因。</li></ul>\n<p>限制：</p>\n<ul><li><code>--dry-run</code>、<code>--only-fetch</code>、<code>--only-filter</code> 不能同时使用。</li></ul>\n<p>示例：</p>\n<div class=\"code-block\"><pre><code>pr-review https://github.com/owner/repo/pull/123\npr-review https://github.com/owner/repo/pull/123 --format markdown --output review.md\npr-review https://github.com/owner/repo/pull/123 --only-filter --show-filter-reasons\npr-review https://github.com/owner/repo/pull/123 --publish-comment</code></pre></div>\n<h4><code>pr-review config</code></h4>\n<p>启动交互式配置向导。</p>\n<p>选项：</p>\n<ul><li><code>--quick</code>：使用最小化向导。</li><li><code>--advanced</code>：额外询问 headers 和 extra params。</li><li><code>--save-key</code>：允许把 API Key 明文保存到配置文件。</li></ul>\n<p>说明：</p>\n<ul><li>不带子命令执行时，会直接进入交互向导。</li><li>配置文件默认路径由 <code>DEFAULT_CONFIG_PATH</code> 决定。</li></ul>\n<h4><code>pr-review config show</code></h4>\n<p>输出当前持久化配置，<code>api_key</code> 会做脱敏处理。</p>\n<p>示例输出字段：</p>\n<ul><li><code>config_path</code></li><li><code>model_provider.name</code></li><li><code>model_provider.model_name</code></li><li><code>model_provider.base_url</code></li><li><code>model_provider.api_format</code></li></ul>\n<h4><code>pr-review config test</code></h4>\n<p>校验当前 provider 配置是否合法。</p>\n<p>成功时输出 provider、model 和 format；失败时以 CLI 错误退出。</p>"},{"id":"project-structure","label":"项目结构","title":"README · 代码结构","source":"README.md","html":"<div class=\"code-block\"><pre><code>src/ai_pr_review/\n  cli.py                      # CLI 入口与主流程编排\n  config.py                   # 配置加载与 provider 预设\n  models/pr_data.py           # PR 数据模型\n  services/\n    pr_fetcher.py             # GitHub PR 获取\n    filter_pipeline.py        # 文件过滤\n    context_builder.py        # 上下文构建\n    prompt_assembler.py       # Prompt 与输出 schema\n    ai_client.py              # 模型调用与成本控制\n    post_processor.py         # finding 后处理\n    report_renderer.py        # 终端 / Markdown / JSON / comment 渲染\n    result_store.py           # SQLite 存储层\n    review_orchestrator.py    # 主流程编排与结果持久化</code></pre></div>"},{"id":"workflow-guide","label":"PR 工作流","title":"PR Workflow Guide","source":"docs/PR_WORKFLOW.md","html":"<p># PR 工作流指南</p>\n<p>本项目采用“小而专一”的 Pull Request 工作方式。目标是让 <code>main</code> 分支始终保持可运行，同时让每次改动都容易审查、容易回滚、容易定位问题。</p>\n<p>## 规则</p>\n<ul><li>所有新功能、修复和文档更新都通过 PR 合入。</li><li>每个 PR 只聚焦一个明确目标：一个功能、一个修复或一组相关文档变更。</li><li>大任务必须拆成多个可以独立审查和合并的小 PR。</li><li>PR 标题和描述必须与实际代码改动一致。</li><li>不要提交空的 PR 描述。</li><li>如果新增了第三方依赖，必须同步更新 <code>README.md</code>。</li><li>如果复用了历史代码或外部片段，需要在 PR 描述中明确来源。</li><li>每次合并后都要确保 <code>main</code> 仍可运行。</li></ul>\n<p>## 推荐分支流程</p>\n<p>开始开发前：</p>\n<div class=\"code-block\"><pre><code>git switch main\ngit pull --ff-only origin main\ngit switch -c feat/small-focused-change</code></pre></div>\n<p>实现并验证完成后：</p>\n<div class=\"code-block\"><pre><code>git status\ngit diff\ngit add &lt;intended-files&gt;\ngit commit -m &quot;feat: describe one focused change&quot;\ngit push -u origin feat/small-focused-change</code></pre></div>\n<p>然后向 <code>main</code> 发起 Pull Request。</p>\n<p>## PR 描述模板</p>\n<div class=\"code-block\"><pre><code>## 功能描述\n\n说明本 PR 新增、修改或修复了什么，以及它解决的问题。\n\n## 使用方式\n\n示例：`pr-review ...`\n\n## 实现思路\n\n- 简要说明核心实现逻辑。\n- 说明关键技术选择。\n- 如复用了历史代码或外部片段，注明来源。\n\n## 测试方式\n\n示例：`python -m pytest tests/test_cli.py -k &quot;focused_keyword&quot;`\n\n## 依赖说明\n\n- 是否新增第三方依赖：否\n- 如有新增依赖，说明 README 中的对应更新。\n\n## 原创性说明\n\n本 PR 为本项目内新增实现，未复用外部代码。</code></pre></div>\n<p>## 建议的 PR 拆分方式</p>\n<ul><li><code>fix:</code> 保持行为稳定的缺陷修复。</li><li><code>feat:</code> 每个 PR 只做一个用户可感知功能。</li><li><code>docs:</code> 仅文档改动。</li><li><code>test:</code> 聚焦测试覆盖与验证补充。</li><li><code>chore:</code> 不改变行为的仓库维护事项。</li></ul>\n<p>## 提交前验证清单</p>\n<p>在创建 PR 之前，先运行最小相关测试，再根据改动范围扩大验证。</p>\n<p>常用命令：</p>\n<div class=\"code-block\"><pre><code>python -m pytest tests/test_cli.py\npython -m pytest tests/test_result_store.py\npython -m pytest</code></pre></div>\n<p>同时检查：</p>\n<ul><li><code>git status --short</code></li><li><code>git diff --stat</code></li><li>暂存区中没有密钥、token 或本地配置文件。</li><li>PR 描述与实际 diff 保持一致。</li></ul>"}],"references":[{"title":"README.md","description":"安装、配置、技术栈、路线图与真实能力边界。","url":"https://github.com/JiangLai999/AI-PR-Review-Assistant/blob/main/README.md"},{"title":"docs/API.md","description":"CLI 命令、参数、输出格式与退出行为。","url":"https://github.com/JiangLai999/AI-PR-Review-Assistant/blob/main/docs/API.md"},{"title":"docs/PR_WORKFLOW.md","description":"Pull Request 流程、分支建议、模板和验证清单。","url":"https://github.com/JiangLai999/AI-PR-Review-Assistant/blob/main/docs/PR_WORKFLOW.md"},{"title":"docs/RELEASE.md","description":"发布、分发与版本管理流程。","url":"https://github.com/JiangLai999/AI-PR-Review-Assistant/blob/main/docs/RELEASE.md"},{"title":"CONTRIBUTING.md","description":"贡献规范、协作方式与提交建议。","url":"https://github.com/JiangLai999/AI-PR-Review-Assistant/blob/main/CONTRIBUTING.md"}]};
