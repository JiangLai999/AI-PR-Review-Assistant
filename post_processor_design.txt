请实现 AI PR Review 助手的 Post-Processor 模块。

## 模块职责

1. JSON Schema 校验
2. 置信度过滤（< 0.6 自动丢弃）
3. 同文件同模式去重
4. 结果排序（按严重程度）
5. 格式化输出

## 核心接口

```python
class PostProcessor:
    def __init__(self, config: PostProcessorConfig):
        pass

    def process(self, result: ReviewResult) -> ReviewResult:
        """后处理 Review 结果"""
        pass

    def filter_by_confidence(self, findings: list[Finding], threshold: float) -> list[Finding]:
        """按置信度过滤"""
        pass

    def deduplicate(self, findings: list[Finding]) -> list[Finding]:
        """去重"""
        pass

    def sort_by_severity(self, findings: list[Finding]) -> list[Finding]:
        """按严重程度排序"""
        pass
```

## 处理规则

1. 置信度过滤：confidence < 0.6 的 finding 丢弃
2. 去重规则：同文件 + 同类别 + 相近行号（行号 // 10 相同）
3. 排序规则：critical > high > medium > low > info
4. 格式化：确保输出符合 JSON Schema

## 要求

1. 实现完整的 PostProcessor 类
2. 支持可配置的阈值
3. 支持自定义去重规则
4. 创建以下文件：
   - src/ai_pr_review/services/post_processor.py
   - src/ai_pr_review/config.py（更新）
   - tests/test_post_processor.py
5. 确保测试通过

请开始实现！