请完成 AI PR Review 助手阶段 3 的架构优化任务。

## 阶段 3 任务清单

### 3.1 引入 orchestrator
- 抽取 ReviewOrchestrator 类
- CLI 只负责参数解析和调用
- 支持 FastAPI、GitHub Action、定时任务复用

### 3.2 做受控并发性能优化
- 引入受控并发（asyncio.Semaphore）
- 文件内容获取可并发
- AI 调用可用 Semaphore 控制并发数
- 同时评审 2-4 个文件

### 3.3 决定 tree-sitter 策略
- 选项 A：真正实现 tree-sitter（3-7 天）
- 选项 B：降级为 roadmap，承认 regex-first

### 3.4 补 CLI 高级能力
- 增加 --dry-run
- 增加 --only-fetch
- 增加 --only-filter
- 增加 --show-filter-reasons
- 增加 pr-review stats

## 优先级

1. 引入 orchestrator（最重要）
2. 补 CLI 高级能力
3. 做受控并发性能优化
4. 决定 tree-sitter 策略

请按优先级依次完成这些任务，确保所有测试通过。